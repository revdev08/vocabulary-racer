import { Pressable, Text, View } from 'react-native';
import type { JourneyLevel } from '../../game/data/journey';
import { green, ink, PaperSurface, type TicketState } from './LevelTicket';
export function SelectedLevelPanel({ level, state, target, requirement, disabled, bottom, onPlay }: { level: JourneyLevel; state: TicketState; target: number; requirement: string; disabled: boolean; bottom: number; onPlay: () => void }) {
  return <View style={{ marginHorizontal: 9, marginBottom: Math.max(bottom, 8), padding: 20, paddingBottom: 17 }}><PaperSurface active perforation={false}/><Text style={{ color: green, fontSize: 10, fontWeight: '800', letterSpacing: 1 }}>BOLETO DE VIAJE · NIVEL {level.number}</Text><Text style={{ color: ink, fontSize: 20, fontWeight: '900', marginTop: 3 }}>{level.title}</Text><Text style={{ color: '#485D65', fontSize: 12, marginTop: 5 }}>{target} preguntas base + hasta 3 de repaso · 3 vidas</Text><Text style={{ color: '#485D65', fontSize: 12, marginTop: 3 }}>Meta: {Math.ceil(target * .8)}/{target} al primer intento y terminar la carrera.</Text><Pressable accessibilityRole="button" accessibilityState={{ disabled: disabled || state === 'locked' }} disabled={disabled || state === 'locked'} onPress={onPlay} style={({ pressed }) => ({ marginTop: 11, minHeight: 48, padding: 12, justifyContent: 'center', alignItems: 'center', borderRadius: 24, backgroundColor: disabled || state === 'locked' ? '#E1DFD3' : green, opacity: pressed ? .8 : 1 })}><Text style={{ color: disabled || state === 'locked' ? '#526167' : '#FFFBEF', fontSize: 16, fontWeight: '800', textAlign: 'center' }}>{level.development ? 'Catálogo de prueba · Inicio deshabilitado' : state === 'locked' ? requirement : state === 'completed' ? 'Repetir nivel  ›' : 'Jugar nivel  ›'}</Text></Pressable></View>;
}
