export const gameplay = {
  initialLives: 3,
  pointsPerCorrect: 100,
  // Encounter times describe arrivals at the player, not spawn times.
  firstEncounterSeconds: 2.0,
  encounterGapSeconds: 1.05,
  minimumEncounterGapSeconds: 0.60,
  gapReductionPerRound: 0.09,
  speedIncreasePerRound: 0.3,
  maximumTrafficSpeed: 3.3,
  encounterJitterSeconds: 0.07,
  burstGapFactor: 0.82,
  recoveryGapFactor: 1.25,
  roundsForBursts: 1,
  beginnerExtraSeconds: 0.1,
  doubleChangeLeadSeconds: 1.08,
  reactionSeconds: 0.26,
  gestureRepeatSeconds: 0.18,
  // Added after the actual rear clearance, which shrinks with relative speed.
  maneuverClearanceSeconds: 0.04,
  fallbackGapSeconds: 1.3,
  routeTolerance: 0.035,
  routeStepSeconds: 1 / 60,
  clearAfterEncounterSeconds: 0.27,
  slowdownSeconds: 0.5,
  plannerAttempts: 8,
  maxObjects: 16,
  simpleEncounters: 6,
  advancedEncounters: 8,
  roundsForCrossovers: 2,
  decisionSeconds: 4,
  decisionSpeed: 0.18,
  feedbackSeconds: 1.15,
  wrongFeedbackSeconds: 1.75,
  portalExitFadeSeconds: 0.55,
  collisionProtectionSeconds: 1.4,
  collisionFeedbackSeconds: 0.75,
  trafficCarSpeed: 0.2,
  reviewDelayQuestions: 3,
  maxStepSeconds: 1 / 120,
  passedObjectDistance: 0.85,
  coinsPerLine: 2,
  coinSpacingSeconds: 0.30,
  maxCoins: 4,
  coinHalfWidth: 0.11,
  coinDepth: 0.07,
  coinRadius: 0.14,
  maxEffects: 8,
  coinFlashSeconds: 0.4,
  answerEffectSeconds: 1.15,
  nitroEveryCorrect: 3,
  nitroSeconds: 2,
  playerHalfWidth: 0.38,
  playerFront: 0.15,
  playerRear: 0.055,
} as const;

// Opaque bounds measured from the approved PNGs (alpha > 32), not the canvas.
export const objectVisuals = {
  traffic: { left: 54 / 1254, right: 1200 / 1254, top: 198 / 1254, bottom: 1070 / 1254,
    visibleWidth: 0.84, halfWidth: 0.36, front: 0.075, rear: 0.08 },
  barrier: { left: 18 / 512, right: 494 / 512, top: 154 / 512, bottom: 386 / 512,
    visibleWidth: 0.88, halfWidth: 0.39, front: 0.025, rear: 0.06 },
} as const;

export const rewardVisuals = {
  streakPulseSeconds: 0.35,
  streakPulseScale: 0.13,
  wordEntrySeconds: 0.18,
  coinParticles: 5,
  answerParticles: 12,
  nitroLines: 6,
  nitroFadeInSeconds: 0.12,
  nitroFadeOutSeconds: 0.3,
} as const;
